import React, { FC } from "react";
import AppRouter from "../app-router";
import "../ui/styles/semantic.min.css";

const App: FC = () => {
    return (
        <AppRouter />
    );
}

export default App;
